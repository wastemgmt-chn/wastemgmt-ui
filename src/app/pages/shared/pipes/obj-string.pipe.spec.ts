import { ObjStringPipe } from './obj-string.pipe';

describe('ObjStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjStringPipe();
    expect(pipe).toBeTruthy();
  });
});
